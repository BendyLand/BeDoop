/* 
ENCODING:
    rot13
    base64 encode
    base64 decode
    md5 checksum (computes the checksum of the text (hex encoded))
    html encode all characters
    url entity encode
    url entities decode
    sha1 hash
    sha256 hash
    sha512 hash 
*/

